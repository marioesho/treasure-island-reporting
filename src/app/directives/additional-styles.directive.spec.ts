import { AdditionalStylesDirective } from './additional-styles.directive';

describe('AdditionalStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new AdditionalStylesDirective();
    expect(directive).toBeTruthy();
  });
});
